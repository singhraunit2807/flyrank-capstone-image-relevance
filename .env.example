# Application
APP_ENV=development

# Optional Gemini provider configuration (never commit a real key)
GEMINI_API_KEY=

# Optional database configuration for the PostgreSQL production layer
DATABASE_URL=postgresql://flyrank:flyrank@localhost:5432/flyrank
